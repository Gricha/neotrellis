#ifndef Intro_h
#define Intro_h

#include "../board_driver.h"
#include "../core/arduino_header.h"

/**
 * The animation that is currently played at intro.
 */
void randomized_intro(BoardDriver &driver);

#endif